import { useRouter } from "next/router";
export const HeaderOnly = () => {
    const router = useRouter()

    const handleRedirect = () => { router.push('/Login'); // Replace with your desired path };
  }
};
  

  